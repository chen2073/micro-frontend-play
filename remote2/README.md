# This is a react remote component

React Virtual DOM must be rendered into raw HTML string using React's renderToString function, because Vue does not have a way to interpret React' Virtual DOM object.
